# Feature Flags

When adding or modifying feature flags, keep the feature flag map sorted alphabetically by flag key.

In CRM this applies to `apps/crm/src/config/featureFlag.ts`.

## Related
- [App-Specific Patterns](app-specific-patterns.md) — feature flag gates for nav-link access in MC1
